If we want to use AngularJS in our html code we have to do 2 steps:
	1) Add <script src="angular.min.js"></script> in our code
	2) Use ng-app attribute to manipulate expressions like this
		 5 + 5 = {{ 5 + 5}}

It is better to add ng-app attribute in html tag like this <html ng-app>